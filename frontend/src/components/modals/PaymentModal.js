const MakePayment = ({ statement }) => {
  console.log("MakePayment -> statement");

  return (
    <div>
      <button className="material-symbols-outlined">payments</button>
    </div>
  );
};

export default MakePayment;
